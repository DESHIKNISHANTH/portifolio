<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>3D Portfolio - Deshik Nishanth</title>
  <link
    href="https://fonts.googleapis.com/css2?family=Syne:wght@400;700;800&family=DM+Sans:wght@300;400;500&display=swap"
    rel="stylesheet">
  <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/gh/devicons/devicon@latest/devicon.min.css" />
  <style>
    :root {
      --neon-cyan: #00f5ff;
      --neon-pink: #ff0080;
      --neon-purple: #7b2fff;
      --neon-gold: #ffd700;
      --bg-dark: #050510;
      --bg-card: rgba(255, 255, 255, 0.04);
      --text-main: #f0f0ff;
      --text-muted: #8888bb;
    }

    [data-theme="light"] {
      --neon-cyan: #0088b3;
      --neon-pink: #cc0066;
      --neon-purple: #5c18cc;
      --neon-gold: #b39500;
      --bg-dark: #f4f6fa;
      --bg-card: rgba(0, 0, 0, 0.05);
      --text-main: #1a1a2e;
      --text-muted: #555577;
    }

    [data-theme="light"] .stars {
      opacity: 0.4;
      filter: invert(1);
    }

    [data-theme="light"] nav {
      background: rgba(244, 246, 250, 0.8);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'DM Sans', sans-serif;
      background: var(--bg-dark);
      color: var(--text-main);
      overflow-x: hidden;
      cursor: none;
    }

    /* Custom cursor */
    .cursor {
      position: fixed;
      width: 18px;
      height: 18px;
      border: 2px solid var(--neon-cyan);
      border-radius: 50%;
      pointer-events: none;
      z-index: 9999;
      transform: translate(-50%, -50%);
      transition: transform 0.1s, background 0.2s;
      mix-blend-mode: difference;
    }

    .cursor-dot {
      position: fixed;
      width: 6px;
      height: 6px;
      background: var(--neon-pink);
      border-radius: 50%;
      pointer-events: none;
      z-index: 9999;
      transform: translate(-50%, -50%);
    }

    /* Animated background */
    .bg-anim {
      position: fixed;
      inset: 0;
      z-index: 0;
      background: radial-gradient(ellipse at 20% 20%, rgba(123, 47, 255, 0.18) 0%, transparent 60%),
        radial-gradient(ellipse at 80% 80%, rgba(0, 245, 255, 0.12) 0%, transparent 60%),
        radial-gradient(ellipse at 60% 10%, rgba(255, 0, 128, 0.10) 0%, transparent 50%);
      animation: bgPulse 8s ease-in-out infinite alternate;
    }

    @keyframes bgPulse {
      0% {
        opacity: 0.7;
      }

      100% {
        opacity: 1;
      }
    }

    /* Theme Toggle */
    .theme-toggle {
      background: transparent;
      border: 1px solid var(--neon-cyan);
      color: var(--text-main);
      width: 40px;
      height: 40px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.2rem;
      cursor: none;
      transition: all 0.3s;
    }

    .theme-toggle:hover {
      background: var(--neon-cyan);
      color: var(--bg-dark);
      box-shadow: 0 0 15px rgba(0, 245, 255, 0.4);
    }

    /* Stars */
    .stars {
      position: fixed;
      inset: 0;
      z-index: 0;
      background-image:
        radial-gradient(1px 1px at 10% 15%, rgba(255, 255, 255, 0.6) 0%, transparent 100%),
        radial-gradient(1px 1px at 30% 70%, rgba(0, 245, 255, 0.5) 0%, transparent 100%),
        radial-gradient(1px 1px at 50% 30%, rgba(255, 255, 255, 0.4) 0%, transparent 100%),
        radial-gradient(1px 1px at 70% 60%, rgba(255, 0, 128, 0.5) 0%, transparent 100%),
        radial-gradient(1px 1px at 90% 20%, rgba(255, 255, 255, 0.5) 0%, transparent 100%),
        radial-gradient(1px 1px at 20% 90%, rgba(0, 245, 255, 0.4) 0%, transparent 100%),
        radial-gradient(1px 1px at 60% 80%, rgba(255, 255, 255, 0.3) 0%, transparent 100%),
        radial-gradient(1px 1px at 80% 40%, rgba(123, 47, 255, 0.6) 0%, transparent 100%);
      background-size: 600px 600px;
      animation: starDrift 20s linear infinite;
    }

    @keyframes starDrift {
      from {
        background-position: 0 0;
      }

      to {
        background-position: 600px 600px;
      }
    }

    /* Nav */
    nav {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 100;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px 60px;
      background: rgba(5, 5, 16, 0.7);
      backdrop-filter: blur(20px);
      border-bottom: 1px solid rgba(0, 245, 255, 0.1);
    }

    .logo {
      font-family: 'Syne', sans-serif;
      font-weight: 800;
      font-size: 1.5rem;
      background: linear-gradient(90deg, var(--neon-cyan), var(--neon-pink));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      letter-spacing: -1px;
    }

    .nav-links {
      display: flex;
      gap: 32px;
      list-style: none;
    }

    .nav-links a {
      color: var(--text-muted);
      text-decoration: none;
      font-size: 0.85rem;
      letter-spacing: 1px;
      text-transform: uppercase;
      transition: color 0.3s;
      position: relative;
    }

    .nav-links a::after {
      content: '';
      position: absolute;
      bottom: -4px;
      left: 0;
      right: 0;
      height: 1px;
      background: var(--neon-cyan);
      transform: scaleX(0);
      transition: transform 0.3s;
    }

    .nav-links a:hover {
      color: var(--neon-cyan);
    }

    .nav-links a:hover::after {
      transform: scaleX(1);
    }

    /* Hero */
    .hero {
      position: relative;
      z-index: 1;
      min-height: 100vh;
      display: flex;
      align-items: center;
      padding: 120px 60px 60px;
    }

    .hero-content {
      max-width: 700px;
    }

    .hero-tag {
      display: inline-block;
      padding: 6px 16px;
      border: 1px solid rgba(0, 245, 255, 0.3);
      border-radius: 20px;
      font-size: 0.75rem;
      letter-spacing: 2px;
      text-transform: uppercase;
      color: var(--neon-cyan);
      margin-bottom: 24px;
      animation: fadeSlideUp 0.8s ease forwards;
      opacity: 0;
    }

    .hero h1 {
      font-family: 'Syne', sans-serif;
      font-size: clamp(3rem, 7vw, 6rem);
      font-weight: 800;
      line-height: 1;
      margin-bottom: 24px;
      animation: fadeSlideUp 0.8s 0.2s ease forwards;
      opacity: 0;
    }

    .hero h1 .line1 {
      display: block;
      color: var(--text-main);
    }

    .hero h1 .line2 {
      display: block;
      background: linear-gradient(90deg, var(--neon-pink), var(--neon-purple), var(--neon-cyan));
      background-size: 200%;
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      animation: gradShift 4s linear infinite;
    }

    @keyframes gradShift {
      0% {
        background-position: 0%;
      }

      100% {
        background-position: 200%;
      }
    }

    .hero p {
      color: var(--text-muted);
      font-size: 1.1rem;
      line-height: 1.7;
      margin-bottom: 40px;
      max-width: 500px;
      animation: fadeSlideUp 0.8s 0.4s ease forwards;
      opacity: 0;
    }

    .hero-btns {
      display: flex;
      gap: 16px;
      flex-wrap: wrap;
      animation: fadeSlideUp 0.8s 0.6s ease forwards;
      opacity: 0;
    }

    .btn-primary {
      padding: 14px 36px;
      background: linear-gradient(135deg, var(--neon-pink), var(--neon-purple));
      color: white;
      border: none;
      font-family: 'DM Sans', sans-serif;
      font-size: 0.95rem;
      font-weight: 500;
      cursor: none;
      clip-path: polygon(12px 0%, 100% 0%, calc(100% - 12px) 100%, 0% 100%);
      transition: all 0.3s;
      box-shadow: 0 8px 32px rgba(255, 0, 128, 0.3);
    }

    .btn-primary:hover {
      transform: translateY(-3px);
      box-shadow: 0 16px 40px rgba(255, 0, 128, 0.5);
    }

    .btn-outline {
      padding: 14px 36px;
      border: 1px solid rgba(255, 255, 255, 0.2);
      background: transparent;
      color: var(--text-main);
      font-family: 'DM Sans', sans-serif;
      font-size: 0.95rem;
      cursor: none;
      clip-path: polygon(12px 0%, 100% 0%, calc(100% - 12px) 100%, 0% 100%);
      transition: all 0.3s;
    }

    .btn-outline:hover {
      border-color: var(--neon-cyan);
      color: var(--neon-cyan);
    }

    /* 3D Floating card */
    .hero-3d {
      position: absolute;
      right: 60px;
      top: 50%;
      transform: translateY(-50%);
      width: 380px;
      height: 480px;
      animation: float3D 6s ease-in-out infinite;
      perspective: 800px;
    }

    @keyframes float3D {

      0%,
      100% {
        transform: translateY(-50%) rotateY(-8deg) rotateX(4deg);
      }

      50% {
        transform: translateY(calc(-50% - 20px)) rotateY(8deg) rotateX(-4deg);
      }
    }

    .card-3d {
      width: 100%;
      height: 100%;
      background: linear-gradient(135deg, rgba(123, 47, 255, 0.2), rgba(0, 245, 255, 0.1));
      border: 1px solid rgba(0, 245, 255, 0.2);
      border-radius: 24px;
      backdrop-filter: blur(20px);
      box-shadow: 0 40px 80px rgba(0, 0, 0, 0.5), inset 0 1px 0 rgba(255, 255, 255, 0.1),
        0 0 60px rgba(0, 245, 255, 0.05);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 16px;
      padding: 32px;
      position: relative;
      overflow: hidden;
    }

    .card-3d::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: conic-gradient(from 0deg, transparent, rgba(0, 245, 255, 0.05), transparent 60%);
      animation: cardSpin 8s linear infinite;
    }

    @keyframes cardSpin {
      from {
        transform: rotate(0deg);
      }

      to {
        transform: rotate(360deg);
      }
    }

    .avatar-ring {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      position: relative;
      background: linear-gradient(135deg, var(--neon-pink), var(--neon-purple), var(--neon-cyan));
      padding: 3px;
      animation: ringGlow 3s ease-in-out infinite alternate;
    }

    @keyframes ringGlow {
      from {
        box-shadow: 0 0 20px rgba(255, 0, 128, 0.4);
      }

      to {
        box-shadow: 0 0 40px rgba(0, 245, 255, 0.6);
      }
    }

    .avatar-inner {
      width: 100%;
      height: 100%;
      border-radius: 50%;
      background: linear-gradient(135deg, #1a1a2e, #2d1b69);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 3rem;
    }

    .card-name {
      font-family: 'Syne', sans-serif;
      font-weight: 700;
      font-size: 1.3rem;
      text-align: center;
    }

    .card-role {
      color: var(--neon-cyan);
      font-size: 0.8rem;
      letter-spacing: 2px;
      text-transform: uppercase;
      text-align: center;
    }

    .card-stats {
      display: flex;
      gap: 24px;
      margin-top: 8px;
    }

    .stat {
      text-align: center;
    }

    .stat-num {
      font-family: 'Syne', sans-serif;
      font-weight: 800;
      font-size: 1.4rem;
      background: linear-gradient(135deg, var(--neon-gold), var(--neon-pink));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .stat-label {
      font-size: 0.7rem;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .card-tags {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      justify-content: center;
    }

    .tag {
      padding: 4px 12px;
      font-size: 0.7rem;
      border-radius: 20px;
      border: 1px solid rgba(255, 255, 255, 0.1);
      color: var(--text-muted);
      background: rgba(255, 255, 255, 0.04);
    }

    .tag.active {
      border-color: var(--neon-pink);
      color: var(--neon-pink);
      background: rgba(255, 0, 128, 0.08);
    }

    /* Sections */
    section {
      position: relative;
      z-index: 1;
      padding: 100px 60px;
    }

    .section-label {
      font-size: 0.75rem;
      letter-spacing: 3px;
      text-transform: uppercase;
      color: var(--neon-cyan);
      margin-bottom: 12px;
    }

    .section-title {
      font-family: 'Syne', sans-serif;
      font-size: clamp(2rem, 4vw, 3.5rem);
      font-weight: 800;
      line-height: 1.1;
      margin-bottom: 16px;
    }

    .section-sub {
      color: var(--text-muted);
      max-width: 500px;
      line-height: 1.7;
      margin-bottom: 60px;
    }

    /* Skills Layout */
    .skills-container {
      display: flex;
      flex-direction: column;
      gap: 40px;
      margin-top: 40px;
      max-width: 900px;
      margin-left: auto;
      margin-right: auto;
    }

    .skill-category-title {
      font-family: 'DM Sans', sans-serif;
      font-size: 1.1rem;
      font-weight: 500;
      margin-bottom: 20px;
      display: flex;
      align-items: center;
      gap: 12px;
      color: var(--neon-cyan);
    }

    .skill-category-title::before {
      content: '';
      display: block;
      width: 6px;
      height: 6px;
      border-radius: 50%;
      background: var(--neon-cyan);
    }

    .skill-items-grid {
      display: flex;
      flex-wrap: wrap;
      gap: 16px;
    }

    .skill-item {
      background: rgba(255, 255, 255, 0.02);
      border: 1px solid rgba(255, 255, 255, 0.05);
      border-radius: 12px;
      padding: 16px;
      width: 100px;
      height: 100px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 12px;
      transition: all 0.3s;
      cursor: none;
    }

    .skill-item:hover {
      background: rgba(255, 255, 255, 0.06);
      border-color: rgba(0, 245, 255, 0.4);
      transform: translateY(-4px);
    }

    .skill-item i {
      font-size: 2.2rem;
    }

    .skill-item span {
      font-size: 0.75rem;
      color: var(--text-muted);
      font-weight: 500;
      font-family: 'DM Sans', sans-serif;
    }

    .skill-pill {
      background: rgba(255, 255, 255, 0.03);
      border: 1px solid rgba(255, 255, 255, 0.05);
      border-radius: 8px;
      padding: 10px 20px;
      font-size: 0.85rem;
      color: var(--text-muted);
      transition: all 0.3s;
      cursor: none;
      font-family: 'DM Sans', sans-serif;
    }

    .skill-pill:hover {
      background: rgba(255, 255, 255, 0.08);
      border-color: rgba(0, 245, 255, 0.4);
      color: var(--text-main);
      transform: translateY(-2px);
    }

    [data-theme="light"] .skill-item,
    [data-theme="light"] .skill-pill {
      background: rgba(0, 0, 0, 0.03);
      border-color: rgba(0, 0, 0, 0.05);
    }

    [data-theme="light"] .skill-item:hover,
    [data-theme="light"] .skill-pill:hover {
      background: rgba(0, 0, 0, 0.06);
      border-color: var(--neon-cyan);
    }

    [data-theme="light"] .skill-category-title {
      color: var(--neon-cyan);
    }

    /* Legacy Skills Grid */
    .skills-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 20px;
    }

    .skill-card {
      background: var(--bg-card);
      border: 1px solid rgba(255, 255, 255, 0.06);
      border-radius: 16px;
      padding: 28px;
      cursor: none;
      transition: all 0.4s;
      position: relative;
      overflow: hidden;
    }

    .skill-card::before {
      content: '';
      position: absolute;
      inset: 0;
      background: linear-gradient(135deg, var(--c1), var(--c2));
      opacity: 0;
      transition: opacity 0.4s;
      border-radius: 16px;
    }

    .skill-card:hover {
      transform: translateY(-8px) scale(1.02);
      border-color: transparent;
    }

    .skill-card:hover::before {
      opacity: 0.12;
    }

    .skill-card:hover .skill-icon {
      transform: scale(1.2) rotate(-5deg);
    }

    .skill-icon {
      font-size: 2.5rem;
      margin-bottom: 16px;
      display: block;
      transition: transform 0.3s;
    }

    .skill-name {
      font-family: 'Syne', sans-serif;
      font-weight: 700;
      margin-bottom: 8px;
    }

    .skill-desc {
      font-size: 0.85rem;
      color: var(--text-muted);
      line-height: 1.6;
      margin-bottom: 16px;
    }

    .skill-bar {
      height: 3px;
      background: rgba(255, 255, 255, 0.08);
      border-radius: 2px;
      overflow: hidden;
    }

    .skill-fill {
      height: 100%;
      border-radius: 2px;
      background: linear-gradient(90deg, var(--c1), var(--c2));
      animation: fillBar 2s ease forwards;
      width: 0%;
    }

    @keyframes fillBar {
      to {
        width: var(--pct);
      }
    }

    /* Projects */
    .projects-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      gap: 24px;
    }

    .project-card {
      border-radius: 20px;
      overflow: hidden;
      border: 1px solid rgba(255, 255, 255, 0.06);
      background: var(--bg-card);
      transition: transform 0.4s, box-shadow 0.4s;
      cursor: none;
      position: relative;
      transform-style: preserve-3d;
    }

    .project-card:hover {
      transform: translateY(-12px) rotateX(3deg);
      box-shadow: 0 40px 60px rgba(0, 0, 0, 0.6), 0 0 40px rgba(0, 245, 255, 0.1);
    }

    .project-img {
      height: 200px;
      position: relative;
      overflow: hidden;
    }

    .project-bg {
      width: 100%;
      height: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 5rem;
      transition: transform 0.4s;
    }

    .project-card:hover .project-bg {
      transform: scale(1.1);
    }

    .project-overlay {
      position: absolute;
      inset: 0;
      background: linear-gradient(to bottom, transparent 40%, rgba(5, 5, 16, 0.9) 100%);
    }

    .project-body {
      padding: 24px;
    }

    .project-cat {
      font-size: 0.7rem;
      letter-spacing: 2px;
      text-transform: uppercase;
      margin-bottom: 8px;
    }

    .project-title {
      font-family: 'Syne', sans-serif;
      font-weight: 700;
      font-size: 1.1rem;
      margin-bottom: 8px;
    }

    .project-desc {
      font-size: 0.85rem;
      color: var(--text-muted);
      line-height: 1.6;
      margin-bottom: 16px;
    }

    .project-links {
      display: flex;
      gap: 12px;
      align-items: center;
    }

    .proj-link {
      font-size: 0.8rem;
      text-decoration: none;
      padding: 6px 16px;
      border-radius: 20px;
      border: 1px solid rgba(255, 255, 255, 0.1);
      color: var(--text-muted);
      transition: all 0.3s;
    }

    .proj-link:hover {
      border-color: var(--neon-cyan);
      color: var(--neon-cyan);
    }

    .proj-link.live {
      background: linear-gradient(135deg, var(--neon-pink), var(--neon-purple));
      border: none;
      color: white;
    }

    /* Contact */
    .contact-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 60px;
      align-items: start;
    }

    .contact-info h3 {
      font-family: 'Syne', sans-serif;
      font-size: 1.8rem;
      margin-bottom: 16px;
    }

    .contact-info p {
      color: var(--text-muted);
      line-height: 1.7;
      margin-bottom: 32px;
    }

    .socials {
      display: flex;
      gap: 12px;
    }

    .social-btn {
      width: 48px;
      height: 48px;
      border-radius: 12px;
      background: var(--bg-card);
      border: 1px solid rgba(255, 255, 255, 0.06);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.2rem;
      transition: all 0.3s;
      cursor: none;
      text-decoration: none;
    }

    .social-btn:hover {
      transform: translateY(-4px);
      border-color: var(--neon-cyan);
      box-shadow: 0 8px 24px rgba(0, 245, 255, 0.2);
    }

    .contact-form {
      display: flex;
      flex-direction: column;
      gap: 16px;
    }

    .form-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 16px;
    }

    .form-group {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }

    .form-group label {
      font-size: 0.8rem;
      letter-spacing: 1px;
      color: var(--text-muted);
      text-transform: uppercase;
    }

    .form-group input,
    .form-group textarea {
      background: var(--bg-card);
      border: 1px solid rgba(255, 255, 255, 0.08);
      border-radius: 10px;
      padding: 14px 16px;
      color: var(--text-main);
      font-family: 'DM Sans', sans-serif;
      font-size: 0.9rem;
      outline: none;
      transition: border-color 0.3s, box-shadow 0.3s;
      resize: none;
    }

    .form-group input:focus,
    .form-group textarea:focus {
      border-color: var(--neon-cyan);
      box-shadow: 0 0 16px rgba(0, 245, 255, 0.1);
    }

    .form-group textarea {
      height: 120px;
    }

    /* Divider */
    .divider {
      height: 1px;
      background: linear-gradient(90deg, transparent, rgba(0, 245, 255, 0.2), transparent);
      margin: 0 60px;
    }

    /* Footer */
    footer {
      position: relative;
      z-index: 1;
      padding: 40px 60px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-top: 1px solid rgba(255, 255, 255, 0.04);
    }

    footer p {
      color: var(--text-muted);
      font-size: 0.85rem;
    }

    .footer-grad {
      background: linear-gradient(90deg, var(--neon-cyan), var(--neon-pink));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      font-weight: 600;
    }

    @keyframes fadeSlideUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Scroll reveal */
    .reveal {
      opacity: 0;
      transform: translateY(40px);
      transition: all 0.8s ease;
    }

    .reveal.visible {
      opacity: 1;
      transform: translateY(0);
    }

    /* Scrollbar */
    ::-webkit-scrollbar {
      width: 4px;
    }

    ::-webkit-scrollbar-track {
      background: var(--bg-dark);
    }

    ::-webkit-scrollbar-thumb {
      background: linear-gradient(var(--neon-purple), var(--neon-cyan));
      border-radius: 4px;
    }

    @media (max-width: 900px) {
      nav {
        padding: 16px 24px;
      }

      .nav-links {
        display: none;
      }

      section {
        padding: 60px 24px;
      }

      .hero {
        padding: 100px 24px 60px;
      }

      .hero-3d {
        display: none;
      }

      .contact-grid {
        grid-template-columns: 1fr;
      }

      .divider {
        margin: 0 24px;
      }

      footer {
        flex-direction: column;
        gap: 12px;
        text-align: center;
      }
    }
  </style>
</head>

<body>

  <div class="cursor" id="cursor"></div>
  <div class="cursor-dot" id="cursorDot"></div>
  <div class="bg-anim"></div>
  <div class="stars"></div>

  <!-- NAV -->
  <nav>
    <div class="logo">GDN</div>
    <ul class="nav-links">
      <li><a href="#about">About</a></li>
      <li><a href="#skills">Skills</a></li>
      <li><a href="#projects">Projects</a></li>
      <li><a href="#education">Education</a></li>
      <li><a href="#training">Training</a></li>
      <li><a href="#contact">Contact</a></li>
    </ul>
    <div style="display: flex; gap: 16px; align-items: center;">
      <button class="theme-toggle" id="themeToggle" aria-label="Toggle theme">☀️</button>
    </div>
  </nav>

  <!-- HERO -->
  <section class="hero" id="about">
    <div class="hero-content">
      <div class="hero-tag">✦ Open for opportunities</div>
      <h1>
        <span class="line1">Gudela Deshik</span>
        <span class="line2">Nishanth</span>
      </h1>
      <p>I am a B.Tech student prioritizing AI & Machine Learning, passionate about solving real-world challenges
        through data, algorithms, and full-stack development.</p>
      <div class="hero-btns">
        <a href="#projects" class="btn-outline" style="text-decoration: none;">View My Work</a>
        <a href="https://drive.google.com/file/d/1525168911_Resume_Gudela_Deshik_Nishanth/view?usp=sharing"
          class="btn-outline" style="text-decoration: none;" target="_blank">View CV</a>
        <a href="#contact" class="btn-outline" style="text-decoration: none;">Contact Me</a>
      </div>
    </div>

    <div class="hero-3d">
      <div class="card-3d">
        <div class="avatar-ring">
          <div class="avatar-inner">👨‍💻</div>
        </div>
        <div class="card-name">G. Deshik Nishanth</div>
        <div class="card-role">B.Tech Student (AI & ML)</div>
        <div class="card-stats">
          <div class="stat">
            <div class="stat-num">3+</div>
            <div class="stat-label">Projects</div>
          </div>
          <div class="stat">
            <div class="stat-num">5.55</div>
            <div class="stat-label">CGPA</div>
          </div>
          <div class="stat">
            <div class="stat-num">1</div>
            <div class="stat-label">Hackathons</div>
          </div>
        </div>
        <div class="card-tags">
          <span class="tag active">Python</span>
          <span class="tag active">HTML/CSS/JS</span>
          <span class="tag">TensorFlow</span>
          <span class="tag">React</span>
          <span class="tag">OpenCV</span>
        </div>
      </div>
    </div>
  </section>

  <div class="divider"></div>

  <!-- SKILLS -->
  <section id="skills" style="display: flex; flex-direction: column; align-items: center;">
    <div class="reveal" style="text-align: center;">
      <div class="section-label">What I Know</div>
      <h2 class="section-title"><span
          style="background: linear-gradient(90deg,var(--neon-cyan),var(--neon-pink)); -webkit-background-clip:text; -webkit-text-fill-color:transparent;">Skills
          & Technologies</span>
      </h2>
    </div>

    <div class="skills-container reveal" style="width: 100%;">

      <!-- Languages -->
      <div>
        <div class="skill-category-title">Languages</div>
        <div class="skill-items-grid">
          <div class="skill-item">
            <i class="devicon-c-plain colored"></i>
            <span>C</span>
          </div>
          <div class="skill-item">
            <i class="devicon-cplusplus-plain colored"></i>
            <span>C++</span>
          </div>
          <div class="skill-item">
            <i class="devicon-python-plain colored"></i>
            <span>Python</span>
          </div>
          <div class="skill-item">
            <i class="devicon-java-plain colored"></i>
            <span>Java</span>
          </div>
          <div class="skill-item">
            <i class="devicon-html5-plain colored"></i>
            <span>HTML</span>
          </div>
          <div class="skill-item">
            <i class="devicon-css3-plain colored"></i>
            <span>CSS</span>
          </div>
          <div class="skill-item">
            <i class="devicon-javascript-plain colored"></i>
            <span>JavaScript</span>
          </div>
        </div>
      </div>

      <!-- Frameworks & Libraries -->
      <div>
        <div class="skill-category-title">Frameworks & Libraries</div>
        <div class="skill-items-grid">
          <div class="skill-item">
            <i class="devicon-react-original colored"></i>
            <span>React</span>
          </div>
          <div class="skill-item">
            <i class="devicon-tailwindcss-original colored"></i>
            <span>TailwindCSS</span>
          </div>
          <div class="skill-item">
            <i class="devicon-pandas-plain colored"></i>
            <span>Pandas</span>
          </div>
          <div class="skill-item">
            <i class="devicon-tensorflow-original colored"></i>
            <span>TensorFlow</span>
          </div>
          <div class="skill-item">
            <i class="devicon-scikitlearn-original colored"></i>
            <span>Scikit-Learn</span>
          </div>
        </div>
      </div>

      <!-- Tools -->
      <div>
        <div class="skill-category-title">Tools</div>
        <div class="skill-items-grid">
          <div class="skill-item">
            <i class="devicon-git-plain colored"></i>
            <span>Git</span>
          </div>
          <div class="skill-item">
            <i class="devicon-github-original" style="color: var(--text-main);"></i>
            <span>GitHub</span>
          </div>
          <div class="skill-item">
            <i class="devicon-vscode-plain colored"></i>
            <span>VS Code</span>
          </div>
          <div class="skill-item">
            <i class="devicon-canva-original colored"></i>
            <span>Canva</span>
          </div>
        </div>
      </div>

      <!-- Coding Platforms -->
      <div>
        <div class="skill-category-title">Coding Platforms</div>
        <div class="skill-items-grid">
          <div class="skill-pill">LeetCode</div>
          <div class="skill-pill">GeeksforGeeks</div>
          <div class="skill-pill">CodeChef</div>
          <div class="skill-pill">HackerRank</div>
          <div class="skill-pill">HackerEarth</div>
        </div>
      </div>

    </div>
  </section>

  <div class="divider"></div>

  <!-- PROJECTS -->
  <section id="projects">
    <div class="reveal">
      <div class="section-label">Selected Work</div>
      <h2 class="section-title">Featured <span
          style="background: linear-gradient(90deg,var(--neon-cyan),var(--neon-gold)); -webkit-background-clip:text; -webkit-text-fill-color:transparent;">Projects</span>
      </h2>
      <p class="section-sub">A showcase of systems and applications I've developed.</p>
    </div>
    <div class="projects-grid reveal">
      <div class="project-card">
        <div class="project-img">
          <div class="project-bg" style="background: linear-gradient(135deg,#0d0d2b,#1a0533);">🗃️</div>
          <div class="project-overlay"></div>
        </div>
        <div class="project-body">
          <div class="project-cat" style="color:var(--neon-pink)">Backend / Python</div>
          <div class="project-title">Employee Record System</div>
          <div class="project-desc">A Python console application utilizing file handling and core logic for complete
            CRUD operations on employee data.</div>
          <div class="project-links">
            <a class="proj-link live" href="https://github.com/DESHIKNISHANTH/Employee-Record-system-.git"
              target="_blank">GitHub</a>
          </div>
        </div>
      </div>
      <div class="project-card">
        <div class="project-img">
          <div class="project-bg" style="background: linear-gradient(135deg,#001a0d,#003320);">📷</div>
          <div class="project-overlay"></div>
        </div>
        <div class="project-body">
          <div class="project-cat" style="color:var(--neon-cyan)">Computer Vision</div>
          <div class="project-title">Smart Attendance System</div>
          <div class="project-desc">Face-recognition attendance built with Python and OpenCV, featuring real-time
            detection and CSV data logging.</div>
          <div class="project-links">
            <a class="proj-link live" href="https://github.com/DESHIKNISHANTH/Smart-Attendence-System.git"
              target="_blank">GitHub</a>
          </div>
        </div>
      </div>
      <div class="project-card">
        <div class="project-img">
          <div class="project-bg" style="background: linear-gradient(135deg,#1a1100,#332200);">🌍</div>
          <div class="project-overlay"></div>
        </div>
        <div class="project-body">
          <div class="project-cat" style="color:var(--neon-gold)">Web Development</div>
          <div class="project-title">Environmental Problems</div>
          <div class="project-desc">A responsive, clean UI multipage website highlighting global environmental
            challenges and solutions.</div>
          <div class="project-links">
            <a class="proj-link live" href="https://github.com/DESHIKNISHANTH/Environment-website.git"
              target="_blank">GitHub</a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <div class="divider"></div>

  <!-- EDUCATION -->
  <section id="education">
    <div class="reveal">
      <div class="section-label">Academic Background</div>
      <h2 class="section-title">My <span
          style="background: linear-gradient(90deg,var(--neon-gold),var(--neon-pink)); -webkit-background-clip:text; -webkit-text-fill-color:transparent;">Education</span>
      </h2>
    </div>
    <div class="skills-grid reveal">
      <div class="skill-card" style="--c1:#ffd700; --c2:#ff0080;">
        <span class="skill-icon">🎓</span>
        <div class="skill-name">Lovely Professional University</div>
        <div class="skill-desc" style="margin-bottom: 4px; font-weight: bold; color: var(--text-main);">B.Tech CSE (AI &
          ML)</div>
        <div class="skill-desc">Aug '23 - Present | Phagwara, Punjab<br />CGPA: 5.55</div>
      </div>
      <div class="skill-card" style="--c1:#00f5ff; --c2:#7b2fff;">
        <span class="skill-icon">🏫</span>
        <div class="skill-name">Sri Chaitanya Junior College</div>
        <div class="skill-desc" style="margin-bottom: 4px; font-weight: bold; color: var(--text-main);">Intermediate
          (MPC)</div>
        <div class="skill-desc">Jun '21 - Mar '23 | Visakhapatnam, AP<br />Percentage: 80.5%</div>
      </div>
      <div class="skill-card" style="--c1:#ff0080; --c2:#7b2fff;">
        <span class="skill-icon">🎒</span>
        <div class="skill-name">Sri Chaitanya Techno School</div>
        <div class="skill-desc" style="margin-bottom: 4px; font-weight: bold; color: var(--text-main);">Matriculation
        </div>
        <div class="skill-desc">Apr '20 - Mar '21 | Rajam, AP<br />Percentage: 99.3%</div>
      </div>
    </div>
  </section>

  <div class="divider"></div>

  <!-- TRAINING & CERTIFICATES -->
  <section id="training">
    <div class="reveal">
      <div class="section-label">Continual Learning</div>
      <h2 class="section-title"><span
          style="background: linear-gradient(90deg,var(--neon-cyan),var(--neon-purple)); -webkit-background-clip:text; -webkit-text-fill-color:transparent;">Certifications</span>
        & Training</h2>
    </div>
    <div class="skills-grid reveal">
      <div class="skill-card" style="--c1:#00f5ff; --c2:#7b2fff;">
        <div class="skill-name" style="font-size: 1.1rem;">Python OOP Training</div>
        <div class="skill-desc" style="color: var(--neon-cyan)">CipherSchools | Jul '25</div>
        <div class="skill-desc">35+ hours of training in core OOP concepts (classes, inheritance, polymorphism,
          encapsulation).</div>
      </div>
      <div class="skill-card" style="--c1:#ff0080; --c2:#ffd700;">
        <div class="skill-name" style="font-size: 1.1rem;">Infosys Certifications</div>
        <div class="skill-desc" style="color: var(--neon-pink)">Infosys Springboard | 2025</div>
        <div class="skill-desc">
          • ChatGPT-4 Prompt Engineering<br />
          • Gen AI with No-Code Tools<br />
          • Computational Theory
        </div>
      </div>
      <div class="skill-card" style="--c1:#ffd700; --c2:#00f5ff;">
        <div class="skill-name" style="font-size: 1.1rem;">Webathon Hackathon</div>
        <div class="skill-desc" style="color: var(--neon-gold)">1st Year Achievement</div>
        <div class="skill-desc">Participated and showcased competitive programming skills. Built a complete website from
          scratch.</div>
      </div>
    </div>
  </section>

  <div class="divider"></div>

  <!-- CONTACT -->
  <section id="contact">
    <div class="contact-grid">
      <div class="reveal">
        <div class="section-label">Get In Touch</div>
        <div class="contact-info">
          <h3>Let's <span
              style="background:linear-gradient(90deg,var(--neon-pink),var(--neon-purple)); -webkit-background-clip:text; -webkit-text-fill-color:transparent;">Connect</span>
          </h3>
          <p>I am looking for opportunities and exciting projects. Feel free to reach out to me.</p>
          <p style="color:var(--text-main); font-size:0.9rem;">
            Email: <a href="mailto:deshiknishanth123@gmail.com"
              style="color:var(--neon-cyan); text-decoration:none;">deshiknishanth123@gmail.com</a><br />
            Phone: +91-8341666348
          </p>
          <div class="socials" style="margin-top: 16px;">
            <a class="social-btn" href="https://www.linkedin.com/in/deshiknishanth/" target="_blank">[in]</a>
            <a class="social-btn" href="https://github.com/DESHIKNISHANTH" target="_blank">Git</a>
          </div>
        </div>
      </div>
      <div class="contact-form reveal">
        <div class="form-row">
          <div class="form-group">
            <label>Name</label>
            <input type="text" placeholder="Your name">
          </div>
          <div class="form-group">
            <label>Email</label>
            <input type="email" placeholder="your@email.com">
          </div>
        </div>
        <div class="form-group">
          <label>Subject</label>
          <input type="text" placeholder="Opportunity...">
        </div>
        <div class="form-group">
          <label>Message</label>
          <textarea placeholder="Hi Deshik, let's talk about..."></textarea>
        </div>
        <button class="btn-primary" style="width:fit-content;">Send Message →</button>
      </div>
    </div>
  </section>

  <!-- FOOTER -->
  <footer>
    <p>© 2026 <span class="footer-grad">Gudela Deshik Nishanth</span>. All rights reserved.</p>
    <p style="color:var(--text-muted); font-size:0.8rem;">Built with HTML · CSS · Vanilla JS</p>
  </footer>

  <script>
    // Theme Toggle
    const themeToggle = document.getElementById('themeToggle');
    const currentTheme = localStorage.getItem('theme') || 'dark';

    if (currentTheme === 'light') {
      document.body.setAttribute('data-theme', 'light');
      themeToggle.textContent = '🌙';
    }

    themeToggle.addEventListener('click', () => {
      const isLight = document.body.getAttribute('data-theme') === 'light';
      if (isLight) {
        document.body.removeAttribute('data-theme');
        localStorage.setItem('theme', 'dark');
        themeToggle.textContent = '☀️';
      } else {
        document.body.setAttribute('data-theme', 'light');
        localStorage.setItem('theme', 'light');
        themeToggle.textContent = '🌙';
      }
    });

    // Custom cursor
    const cursor = document.getElementById('cursor');
    const dot = document.getElementById('cursorDot');
    let mx = 0, my = 0, cx = 0, cy = 0;
    document.addEventListener('mousemove', e => {
      mx = e.clientX; my = e.clientY;
      dot.style.left = mx + 'px'; dot.style.top = my + 'px';
    });
    function animCursor() {
      cx += (mx - cx) * 0.12;
      cy += (my - cy) * 0.12;
      cursor.style.left = cx + 'px';
      cursor.style.top = cy + 'px';
      requestAnimationFrame(animCursor);
    }
    animCursor();
    document.querySelectorAll('button, a, .project-card, .skill-card').forEach(el => {
      el.addEventListener('mouseenter', () => {
        cursor.style.transform = 'translate(-50%,-50%) scale(2)';
        cursor.style.background = 'rgba(0,245,255,0.15)';
      });
      el.addEventListener('mouseleave', () => {
        cursor.style.transform = 'translate(-50%,-50%) scale(1)';
        cursor.style.background = 'transparent';
      });
    });

    // Scroll reveal
    const reveals = document.querySelectorAll('.reveal');
    const obs = new IntersectionObserver(entries => {
      entries.forEach((e, i) => {
        if (e.isIntersecting) {
          setTimeout(() => e.target.classList.add('visible'), i * 100);
        }
      });
    }, { threshold: 0.1 });
    reveals.forEach(r => obs.observe(r));

    // Smooth scroll
    document.querySelectorAll('a[href^="#"]').forEach(a => {
      a.addEventListener('click', e => {
        e.preventDefault();
        const t = document.querySelector(a.getAttribute('href'));
        if (t) t.scrollIntoView({ behavior: 'smooth' });
      });
    });

    // 3D tilt on hero card
    const card = document.querySelector('.card-3d');
    if (card) {
      document.addEventListener('mousemove', e => {
        const rx = ((e.clientY / window.innerHeight) - 0.5) * 20;
        const ry = ((e.clientX / window.innerWidth) - 0.5) * 20;
        card.style.transform = `rotateX(${-rx}deg) rotateY(${ry}deg)`;
      });
    }
  </script>
</body>

</html>
